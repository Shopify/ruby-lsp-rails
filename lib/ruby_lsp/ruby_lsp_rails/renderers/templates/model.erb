| | |
|:---|:---|
<%="|Model:".ljust(10," ").gsub(" ","&nbsp;")%>|***<%=name%>***|
<%model.dig(:associations).each  do |k,v|%> <%v.each do |a|%><%="|#{k.to_s.concat(":").ljust(10," ").gsub(" ","&nbsp;").concat("&nbsp;"*5)}| [#{a.dig(:name)}](file://#{a.dig(:file)}) |\n"%><%end%><%end%>
---
---
| Column | | Type | Comment | |
|:------|------|:------|:------|------|
<%model.dig(:columns).each do |column|%> <%="| #{column[:name]} | | #{column[:type]} | #{column[:comment].nil? ? "empty" : column[:comment]} | |\n"%><%end%>
---
<%= model.dig(:schema_file).nil? ? "\n" : "[Schema](#{URI::Generic.build(scheme: "file",path: model.dig(:schema_file))})"%>
